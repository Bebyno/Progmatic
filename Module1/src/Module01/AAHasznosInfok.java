package Module01;

public class AAHasznosInfok {
    /**
     *  ln(new line == enter) nélküle minden 1 sorba kerül.
     *  sout a rövidítése a "System.out.println"-nak
     *  psvm a rövidítése "public static void main"
     *  Scanner sc = new Scanner(System.in)
     *      int num1 = sc.nextInt();        - szám input
     *      String abc = sc.nextLine();     - szöveg input
     *      char word = sc.next().charAt(0); - character input
     *
     *      String charToString = String.valueOf(charName); // char to String
     *
     *  Random rn = new Random();
     *      random.nextInt(max + 1 - min) + min; -> ez az input mechanikája, amikor te adod meg a két értéket
     *                      index szerű, azért kell a +1
     *  Math.random() -> ezzel is létrehozható random szám
     *
     *
     *  Character.isWhitespace(name.charAt(i)) {     -space checker, ez csekkolja le, hogy " "-e az aktuális elem
     *
     * Java-ban az or-nál a || b , ha a bal oldal true, akkor a jobb oldalt már meg sem nézi.
     *
     *
     * .lenght nem index alapú, start az 1ről,2,3,4
     *
     * byte hossza -> -128tól + 127ig
     *
     * Character.isLetter ->megnézi, hogy a karakter betű-e
     *
     * Arrays.toString(név) -- kinyomtatja a tömbödet (kilistázza voltaképp)
     * Arrays.deepToString(név) -- kinyomtattaja a mátrixot
     *
     * __string[] array -> char[] array
     * String stringToArray= "";
     *         for (int i = 0; i < strings.length; i++) {
     *             stringToArray += strings[i];
     *         }
     *         char[] stringToCharArray= stringToArray.toCharArray();
     * __
     *
     *
     * alt + insert (vagy jobb egérgomb és.. )= generator
     */
}

                        // gyorsbillentyűk
/**
 *        ctrl lenyomásával infot ad arról amire ráviszed a kurzort.
 *        crtl + alt + l -> formázás
 *        ctrl + d - a sor copyja
 *        shift + alt + arrow - sor irányba helyezése
 *        ctrl + / - sor kikommentelés
 *        ctrl + shift + / - tömb kikomm
 *        ha egy variabelen refactorálsz a kódban minden azonos nevűt át ír (tehát nem kell minde 1est külön átírni)
 *        ctrl + tab == alt + tab in windows
 *
 *
 */
                // videók hasznos infok

/**
 *         Alapok 210507 -ban 1:19:16 nál mátrix (tekerj vissza)
 *         Alapok 210508 #1 -ban 2:08:00 -nál a sokdimenziós mátrix
 */
