public class HasznosInfos {
    /**
     *  this az az osztályváltozóra mutat.
     *
     * extends -> leszármazás
     *
     *  packig private ->ezt nem írjuk ki, nem irunk semmit, azaz nem  adom meg neki ,
     *  hogy public vagy private vagy bármit, akkor abban a packagen belül az összes osztály eléri az adott változót
     *
     *  protected -> leszármazottak látják.
     *
     *
     * Randomizálás String tömb-ben (összekeveri random az elemeket)
     *
     * public void mixWithFork(Ingredient[] ingredients) {
     *             for (int i = 0; i < ingredients.length; i++) {
     *                 int j = random.nextInt(ingredients.length); // random index szám
     *                 Ingredient helper = ingredients[i]; // Ingredien class-ú tempoláris variable
     *                 ingredients[i] = ingredients[j];
     *                 ingredients[j] = helper;
     *             }
     *         }
     *     }
     *
     *
     * static variable- > nem a példányé hanem az osztályé
     */

}
